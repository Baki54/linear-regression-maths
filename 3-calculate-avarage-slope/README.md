This is not a linear regression algorithm. but it necessary to understand the nexts.
Suppose we have a set of 2 variables data.
we can easily find a slope from two data points. just a simple mathemetical formula.
so we can find the slope of each points with the next point and store these slopes in a list.
Then we can easily find the avarage of these slopes and it could be the trend of the dataset. is it?
Ok its the good concept to understand actual linear regression, but its not a linear regression. so lets go to ne next...
